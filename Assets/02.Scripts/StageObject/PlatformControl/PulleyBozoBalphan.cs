using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 발판을 두 개 밟아야 작동이 되는 퍼즐을 위해 만든 보조 발판.
/// PulleyBalphan의 bozoBalphan에 넣어서 사용합니다.
/// </summary>
public class PulleyBozoBalphan : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
